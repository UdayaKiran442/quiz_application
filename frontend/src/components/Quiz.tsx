import Button from "./ui/Button";

const Quiz = () => {
  return (
    <div>
      <div className="flex justify-end p-4">
        <Button>Create New Quiz</Button>
      </div>
    </div>
  );
};

export default Quiz;
